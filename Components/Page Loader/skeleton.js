import React from "react";
import classes from "./skeleton.module.css";
export const SkeletonLoading = () => {
  return <span className={classes.loader}></span>;
};

export const ProjectLoader = () => {
  return <span className={classes.loader1}></span>;
};
